apiVersion: v1
kind: Service
metadata:
  name: kube-demo-ui
  namespace: kube-demo
spec:
  selector:
    app: kube-demo-ui
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP
